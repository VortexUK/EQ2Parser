<Window x:Class="EQ2Parser.App.Views.ShareOfferWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:EQ2Parser.App.Localization"
        Title="{loc:Tr Share_Title}" Width="560" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BgBrush}">
    <StackPanel Margin="16">
        <TextBlock x:Name="HeaderText" Style="{StaticResource SectionLabel}" Margin="0,0,0,10" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="{loc:Tr Share_Regex}" Style="{StaticResource MutedText}" FontSize="12" VerticalAlignment="Top" Margin="0,2,0,0" />
            <TextBox Grid.Column="1" x:Name="RegexText" IsReadOnly="True" TextWrapping="Wrap"
                     FontFamily="Consolas" FontSize="12" BorderThickness="0"
                     Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource TextBrush}"
                     Padding="6,4" Margin="0,0,0,6" />

            <TextBlock Grid.Row="1" Text="{loc:Tr Share_Sound}" Style="{StaticResource MutedText}" FontSize="12" />
            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="SoundText" Foreground="{StaticResource TextBrush}" FontSize="12.5" Margin="0,0,0,4" TextWrapping="Wrap" />

            <TextBlock Grid.Row="2" Text="{loc:Tr Share_Category}" Style="{StaticResource MutedText}" FontSize="12" />
            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="CategoryText" Foreground="{StaticResource TextBrush}" FontSize="12.5" Margin="0,0,0,4" />

            <TextBlock Grid.Row="3" Grid.ColumnSpan="2" x:Name="TimerNote" Style="{StaticResource MutedText}" FontSize="11.5"
                       TextWrapping="Wrap" Visibility="Collapsed" Margin="0,4,0,0" />
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="{loc:Tr Share_Ignore}" Padding="14,5" Margin="0,0,8,0" Click="Ignore_Click" />
            <Button Content="{loc:Tr Share_Add}" Style="{StaticResource GoldButton}" Padding="16,5" Click="Add_Click" />
        </StackPanel>
    </StackPanel>
</Window>
